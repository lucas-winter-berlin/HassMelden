# Pflicht für /api/generate-complaint (nur serverseitig – kein NEXT_PUBLIC_).
# Key erstellen: https://aistudio.google.com/apikey
# Lokal: Datei als .env.local speichern. Niemals committen.
GEMINI_API_KEY=
